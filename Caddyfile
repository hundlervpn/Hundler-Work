hundlerwork.duckdns.org {
	encode zstd gzip

	# ACME certificate via DuckDNS DNS-01 challenge — robust for DuckDNS domains
	# and does not depend on inbound port 80 reachability.
	tls {
		dns duckdns {env.DUCKDNS_TOKEN}
	}

	reverse_proxy app:3000
}