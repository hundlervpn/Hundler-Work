"use client";import*as React from"react";import{SearchIcon,UsersIcon,HomeIcon,ChatIcon,UserIcon}from"./icons";import type{TabKey,Role}from"@/lib/nav";export function BottomNav({active,onChange,role,unread=0}:{active:TabKey;onChange:(k:TabKey)=>void;role:Role;unread?:number}){const items=[{key:'home' as const,label:'Главная',Icon:HomeIcon},{key:'find' as const,label:'Заказы',Icon:SearchIcon},{key:'work' as const,label:role==='freelancer'?'Отклики':'Мои заказы',Icon:UsersIcon},{key:'chats' as const,label:'Чаты',Icon:ChatIcon},{key:'profile' as const,label:'Профиль',Icon:UserIcon}];return <nav className="fixed inset-x-0 bottom-0 z-40 border-t border-hair bg-surface px-2 pb-[max(8px,env(safe-area-inset-bottom))] pt-2 md:hidden"><div className="mx-auto flex max-w-xl">{items.map(({key,label,Icon})=>{const selected=active===key;return <button key={key==='work'?`${key}-${role}`:key} onClick={()=>onChange(key)} className={`press relative grid min-h-[52px] min-w-0 flex-1 place-items-center gap-1 rounded-2xl px-1 ${key==='work'?'bg-brand-red/10':''}`}><span className="relative"><Icon className={`h-5 w-5 ${selected||key==='work'?'text-brand-red-bright':'text-ink-muted'}`}/>{key==='chats'&&unread>0?<span className="absolute -right-2 -top-2 grid h-4 min-w-4 place-items-center rounded-full bg-brand-red px-1 text-[9px] font-bold text-white">{unread>9?'9+':unread}</span>:null}</span><span className={`truncate text-[10px] font-bold ${selected||key==='work'?'text-brand-red-bright':'text-ink-muted'}`}>{label}</span></button>})}</div></nav>}
