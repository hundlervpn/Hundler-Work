import * as React from "react";
import type { Order } from "@/lib/data";
import { ClockIcon, EyeIcon, UsersIcon, ChevronRight } from "./icons";
export function OrderCard({order,onClick}:{order:Order;onClick?:()=>void}){return <button type="button" onClick={onClick} className="press w-full overflow-hidden rounded-3xl bg-card text-left shadow-border hover:shadow-border-hover">{order.imageUrl?<img src={order.imageUrl} alt="" className="h-40 w-full object-cover"/>:null}<div className="p-5"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><div className="text-xs font-semibold text-brand-red-bright">{order.category}</div><h3 className="mt-1 truncate text-lg font-bold text-ink">{order.title}</h3><p className="mt-1 line-clamp-2 text-sm leading-6 text-ink-muted">{order.subtitle}</p></div><ChevronRight className="mt-2 h-5 w-5 shrink-0 text-ink-muted"/></div><div className="mt-5 flex items-end justify-between gap-3"><div><div className="text-xs uppercase tracking-[.08em] text-ink-muted">Цена</div><div className="tnum mt-1 text-xl font-bold text-brand-red-bright">{order.price} <span className="text-sm">{order.currency}</span></div></div><div className="flex gap-3 text-xs text-ink-muted"><span className="flex items-center gap-1"><ClockIcon className="h-4 w-4"/>{order.days} дн.</span><span className="flex items-center gap-1"><EyeIcon className="h-4 w-4"/>{order.views}</span><span className="flex items-center gap-1"><UsersIcon className="h-4 w-4"/>{order.responses}</span></div></div></div></button>}
