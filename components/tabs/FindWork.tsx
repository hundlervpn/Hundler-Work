"use client";import*as React from"react";import{SearchBar}from"../SearchBar";import{CategorySelect}from"../CategorySelect";import{OrderCard}from"../OrderCard";import{OrderDetail}from"../OrderDetail";import{EmptyState}from"../EmptyState";import type{Order}from"@/lib/data";import{SearchIcon,UsersIcon,WalletIcon}from"../icons";
export function FindWork(){const[items,setItems]=React.useState<Order[]>([]),[open,setOpen]=React.useState<Order|null>(null),[loading,setLoading]=React.useState(true);React.useEffect(()=>{fetch('/api/orders?scope=feed').then(r=>r.json()).then(d=>setItems(d.items||[])).finally(()=>setLoading(false))},[]);if(open)return <OrderDetail order={open} onBack={()=>setOpen(null)}/>;return <div className="stagger"><section className="overflow-hidden rounded-3xl bg-card p-6 shadow-border"><div className="text-xs font-bold uppercase tracking-[.12em] text-brand-red-bright">Hundler Work</div><h1 className="mt-3 text-3xl font-black leading-tight text-ink">Работа и специалисты<br/>в одном месте</h1><p className="mt-3 max-w-md text-sm leading-6 text-ink-muted">Находите заказы, собирайте команду и получайте оплату безопасно через сервис.</p><div className="mt-6 grid grid-cols-3 gap-2 text-center text-xs text-ink-muted"><div><SearchIcon className="mx-auto mb-2 h-5 w-5 text-brand-red-bright"/>Заказы</div><div><UsersIcon className="mx-auto mb-2 h-5 w-5 text-brand-red-bright"/>Специалисты</div><div><WalletIcon className="mx-auto mb-2 h-5 w-5 text-brand-red-bright"/>Безопасная оплата</div></div><a href="https://t.me/hundlerwork" target="_blank" rel="noreferrer" className="press mt-6 flex min-h-[48px] items-center justify-center rounded-2xl bg-brand-red px-4 text-sm font-bold text-white shadow-brand-glow">Перейти в Telegram-канал</a></section><header className="mb-5 mt-8"><h2 className="text-2xl font-bold text-ink">Новые заказы</h2></header><SearchBar/><div className="mt-3"><CategorySelect/></div><div className="mt-6 space-y-4">{loading?<div className="h-40 animate-pulse rounded-3xl bg-card shadow-border"/>:items.length===0?<EmptyState icon={SearchIcon} title="Заказов пока нет" subtitle="Одобренные задания появятся здесь"/>:items.map(o=><OrderCard key={o.id} order={o} onClick={()=>setOpen(o)}/>)}</div></div>}
